    <%= render "navbar", task: @list %>
<%# updated code %>
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path @list.photo.key %>) ">
  <div class="title-container">
    <h1><%= @list.name %></h1>
    <%= link_to "add a comment", new_list_bookmark_path(@list), class: 'add-list-button'%>
  </div>
</div>
<%# end of banner %>
<div class="cards">
  <% @list.bookmarks.each do |bookmark|  %>
    <div class="card" style="width: 30rem;">
      <div class="card-body ">
        <h5 class="card-title"> <%= bookmark.movie.title %></h5>
        <h6 class="card-subtitle mb-2 text-muted"> <%= bookmark.movie.poster_url %> </h6>
        <p class="card-text"> Summary: <%= bookmark.movie.overview %></p>
        <p class="card-text"> Comment: <%= bookmark.comment %></p>
        <%= link_to "delete!", bookmark_path(bookmark),
        data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
      </div>
    </div>
  <% end %>
</div>
<%# <h1><%= @list.name %></h1>
<%# <hr> %>
<%# original code %>
<%# <% @list.bookmarks.each do |bookmark|  %>
  <%# <h2><%= bookmark.movie.title %></h2>
<%# <p> Poster: <%= bookmark.movie.overview %></p>
<%# <p> Poster: <%= bookmark.movie.poster_url %></p>
<%# <p> Comments: <%= bookmark.comment %></p>
<%# <%= link_to "delete!",
    bookmark_path(bookmark),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<%# <% end %>
<%#
<% if @list.photo.attached? %>
<%# <%= cl_image_tag @list.photo.key, width: "200", height: "200" %>
<%# <% else %>
<%# <%= "no photo attached" %>
<%# <% end %>
