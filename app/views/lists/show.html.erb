<h1><%= @list.name %></h1>
<hr>

<% @list.bookmarks.each do |bookmark|  %>
<h2><%= bookmark.movie.title %></h2>
<p> Poster: <%= bookmark.movie.poster_url %></p>
<p> Comments: <%= bookmark.comment %></p>
<%= link_to "delete!",
    bookmark_path(bookmark),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<% end %>
<hr>
<%= link_to "🔙", lists_path, class: 'btn btn-primary' %>
<%= link_to "Bookmarks!", new_list_bookmark_path(@list), class: 'btn btn-primary'%>
